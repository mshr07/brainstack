# Performance testing

Phase 10 adds k6 scenarios for conversation submission, streaming, retrieval and
governed query paths. Scripts will record RPS, error rate, p50/p95/p99 and traced
provider/retrieval/SQL latency against a declared data/configuration snapshot.
This repository makes no benchmark claim before those measurements exist.
